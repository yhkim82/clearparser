#summary Configuration guidelines

The following shows a default configuration file for English ('[http://code.google.com/p/clearparser/source/browse/trunk/config/config_dep_en.xml config/config_dep_en.xml]').

{{{
<configuration>
    <common>
        <language>en</language>
        <format>dep</format>
        <parser>shift-pop</parser>
    </common>
    <classify>
        <algorithm name="lib" l="1" c="0.1" e="0.1" b="-1"/>
        <threads>2</threads>
    </classify>
    <predict>
        <tok_model>model/en_tok_opennlp.jar</tok_model>
        <pos_model>model/en_pos_ontonotes.jar</pos_model>
        <morph_dict>lib/en_dict-1.0.jar</morph_dict>
    </predict>
</configuration>
}}}

<*configuration*> is the top element that consists of 3 elements: <*common*>, <*train*>, and <*predict*>.
<br>

== <*common*> ==

The <*common*> element is required for both training and decoding.  It consists of 2 required elements (*language*, *format*) and 1 optional element (*parser*).

 * <*language*> ::= cz | en | kr
  * Specifies a language (cz: Czech, en: English, kr: Korean)
 * <*format*> ::= pos | dep | srl
  * Specifies a [DataFormat data format]
 * <*parser*> ::= shift-eager | shift-pop
  * Specifies a dependency parsing algorithm (no need for SRL)

== <*train*> ==

The <*train*> element is required for training.  It consists of 1 required element (*algorithm*) and 1 optional element (*threads*).

 * <*algorithm*> specifies a learning algorithm.  It requires an attribute *name*.
  * *name* ::= lib (LibLinear L2-SVM; default) | rrm (Robust Risk Minimization)
  * *l* ::= lib: 1 (L1-loss; default) | 2 (L2-loss)
  * *c* ::= lib: penalty (default = 0.1), rrm: regularization (default = 0.1)
  * *e* ::= lib: termination criterion (default = 0.1), rrm: learning rate (default = 0.001)
  * *b* ::= lib: bias (default = -1)
  * *k* ::= rrm: max # of iterations (default = 40)
  * *m* ::= rrm: initial weights (default = 1.0)
 * <*threads*> ::= # of threads to be used during training (default = 2)

== <*predict*> ==

The <*predict*> element is required for decoding.  It consists of 4 elements (*tok_model*, *pos_model*, *dep_model*, *morph_dict*).

 * <*tok_model*> ::= absolute or relative path
  * Specifies a model file ([https://clearparser.googlecode.com/svn/trunk/model/en_tok_opennlp.jar model/en_tok_opennlp.jar]) for the OpenNLP tokenizer (required for the [DataFormat raw] format).
 * <*pos_model*> ::= absolute or relative path
  * Specifies a model file ([http://verbs.colorado.edu/~choijd/clearparser/models/en_pos_ontonotes.jar model/en_pos_ontonotes.jar]) for the OpenNLP part-of-speech tagger (required for the [DataFormat raw] format).
 * <*morph_dict*> ::= absolute or relative path
  * Specifies a dictionary file ([http://verbs.colorado.edu/~choijd/clearparser/models/en_dict-1.0.jar model/en_dict-1.0.jar]) for the [MorphAnalyzerGuidelines Clear morphological analyzer] (required for the [DataFormat raw, pos] format).